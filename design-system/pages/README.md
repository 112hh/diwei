# Page Overrides

Use `design-system/MASTER.md` by default.

Create a page-specific markdown file here only when a page has a justified exception to the master UI specification. Keep each override narrow and include:

- Page name or route
- Reason for the exception
- Exact token/component rule being changed
- Scope of the change
